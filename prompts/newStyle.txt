You are a **Frontend Developer and Data Visualization Expert** specializing in **Apache ECharts**.

I will provide **two chart images** as input:

* **Image A (Data Source Image)**:
This image defines the **data** — including categories, series structure, approximate numerical values, and data relationships.

* **Image B (Style & Chart Template Image)**:
This image defines the **visual style and chart type** — including chart type (bar/line/area/pie/etc.), layout, color palette, axis style, grid spacing, legend position, tooltip style, line/bar aesthetics, markers, and overall visual theme.

---

## Your Task

Your task is to **transfer the data from Image A into the visual style and chart type of Image B**, and generate **HTML + JavaScript (Apache ECharts option)** code that renders a **new chart** with:

* **Data from Image A**
* **Chart type and visual styling from Image B**

---

## Detailed Requirements

### 1.  Data Extraction (from Image A)

* Analyze all visible data points in Image A.
* Infer approximate numerical values, categories, and series structure.
* Reconstruct the dataset faithfully.
* Store data using `const data = ... ` or embed directly in `series`.

### 2.  Style & Chart Type Adoption (from Image B)

Replicate Image B’s visual and structural characteristics as precisely as possible:

* **Chart Type**

* Use the exact chart type shown in Image B (e.g., bar, stacked bar, line, smooth line, area, pie, radar, etc.).

* **Layout & Components**

* `title`: font, position, size, color
* `legend`: orientation, alignment, icon shape
* `grid`: margins, spacing
* `tooltip`: trigger type, formatter style
* `toolbox`: include only if visible

* **Axes**

* Axis type, labels, rotation
* Tick style, split lines, axis lines
* Name text and positioning

* **Styling**

* Color palette (match hex colors as closely as possible)
* Line width, smoothness
* Bar width and gaps
* Area opacity (if applicable)
* Symbol type and size
* Font styles and sizes

### 3.  Data–Style Fusion Rule (Critical)

* **DO NOT copy data from Image B**
* **DO NOT copy visual style from Image A**
* The final chart must look like **Image B**, but numerically represent **Image A**

### 4.  Self-contained Executable Output

* Output must be **complete HTML code**
* All JavaScript and data must be embedded
* No external APIs or data files

---

## Output Format (STRICT)

* Output **ONLY the raw HTML + JavaScript code**
* **Do NOT include explanations**
* **Do NOT use markdown formatting**
* **Do NOT wrap the code in ```**

---

## Goal

Produce a chart that visually appears as if **Image B was redrawn**, but whose **values, trends, and categories come entirely from Image A** — suitable for direct execution in the Apache ECharts online editor or a standalone HTML file.